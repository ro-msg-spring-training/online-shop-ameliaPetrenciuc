spring.application.name=shop

database.ip=${DB_IP:localhost}
database.port=${DB_PORT:5432}
database.user=${DB_USER}
database.password=${DB_PASSWORD}
database.name=${DB_DBNAME:online-shop}

spring.sql.init.platform=postgres
spring.datasource.url=jdbc:postgresql://${database.ip}:${database.port}/${database.name}
spring.datasource.username=${database.user}
spring.datasource.password=${database.password}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.show_sql=true

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

server.port=${PORT:8080}

logging.level.root=warn
logging.level.org.springframework=warn
logging.level.ro.tuc=warn
#
#order.strategy=singleLocation
#order.strategy=mostAbundant
order.strategy=fallback

jwt.private-key-location=keys/private.pem
jwt.public-key-location=keys/public.pem
jwt.expiration=86400000
